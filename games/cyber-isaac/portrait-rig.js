/* Lightweight 2D portrait deformation, not a Live2D/Cubism model. */
window.NeonPortraitRig={draw(ctx,image,x,y,w,h,time,energy=0){if(!image?.naturalWidth)return;const aspect=image.naturalWidth/image.naturalHeight;w*=Math.min(1,aspect);h*=Math.min(1,1/aspect);ctx.save();ctx.translate(x,y);const rows=24,sw=image.naturalWidth,sh=image.naturalHeight,breath=Math.sin(time*2.1);for(let i=0;i<rows;i++){const v=(i+.5)/rows,cloth=Math.pow(Math.max(0,v-.35),1.3),sway=Math.sin(time*1.6-v*5)*cloth*w*.012,expand=1+Math.sin(v*Math.PI)*breath*.009+energy*.012,sy=i*sh/rows,dh=h/rows;ctx.drawImage(image,0,sy,sw,Math.min(sh-sy,sh/rows+1),-w*expand/2+sway,-h/2+i*dh+Math.sin(time*2.1)*v*1.5,w*expand,dh+1)}ctx.restore()}};
